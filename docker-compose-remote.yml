version: '3.7'

services:
    bot:
        build: https://github.com/edementyev/salesboard.git
        image: salesboard:latest
        command: python3 ./app/bot.py -c -w
        restart: unless-stopped
        environment:
            - SALESBOARD_TOKEN=1165605883:AAFLmgZFDbRNzmFmM_MTKfr7IqeTIB6ZSzU
            - SALESBOARD_ADMIN=['119707338']
            - SALESBOARD_WEBHOOK={'host':'https://static.174.53.203.116.clients.your-server.de'}
        networks:
            - network
        depends_on:
            - db
            - redis

    db:
        image: 'postgres:alpine'
        restart: unless-stopped
        environment:
            - POSTGRES_USER=docker
            - POSTGRES_PASSWORD=docker
            - POSTGRES_DB=docker
        volumes:
            - dbdata:/var/lib/postgresql/data
        networks:
            - network

    redis:
        image: 'redis:alpine'
        command: redis-server --appendonly yes
        restart: unless-stopped
        environment:
            - POSTGRES_USER=docker
            - POSTGRES_PASSWORD=docker
            - POSTGRES_DB=docker
        volumes:
            - redisdata:/data
        networks:
            - network

networks:
    network:

volumes:
    dbdata:
        name: salesboard-db
    redisdata:
        name: salesboard-redis